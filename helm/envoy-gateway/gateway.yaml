apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: connect-gw
  namespace: connect-go
spec:
  gatewayClassName: envoy-gateway
  listeners:
    - name: http
      protocol: HTTP
      port: 80
    # Add HTTPS listener here later if needed
